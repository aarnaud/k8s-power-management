[package]
name = "cpu-power-hal"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Vendor/generation-agnostic Linux CPU power management (EPP, governor, turbo) abstraction"

[dependencies]
thiserror = "2.0.20"
tracing = "0.1.44"

[dev-dependencies]
tempfile = "3.27.0"
